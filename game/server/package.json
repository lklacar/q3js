{
  "name": "@q3js/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm run typecheck && node scripts/build-app.mjs",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "pnpm run build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@fails-components/webtransport": "1.6.7",
    "@fails-components/webtransport-transport-http3-quiche": "1.6.7"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "esbuild": "^0.25.12",
    "typescript": "^5.9.3"
  }
}
